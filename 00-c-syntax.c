// Syntax 
// if...else
// --------------------------
if (condition) {
	// block of code
}
// --------------------------
if (condition) {
	// block of code
} else {
	// block of code	
}
// --------------------------
if (condition1) {
	// block of code
} else if (condition2) {
	// block of code
} else {
	// block of code
}

// Ternary Operator
variable = (condition) ? expressionTrue : expressionFalse;

// Switch
switch(expression) {
    case x:
        // code block
        break;
    case y:
        // code block
        break;
    default:
        // code block
} 

n
